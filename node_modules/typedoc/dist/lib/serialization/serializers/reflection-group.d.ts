import { ReflectionGroup } from '../../models/ReflectionGroup';
import { SerializerComponent } from '../components';
import { Reflection as JSONReflection, ReflectionGroup as JSONReflectionGroup } from '../schema';
export declare class ReflectionGroupSerializer extends SerializerComponent<ReflectionGroup> {
    static PRIORITY: number;
    serializeGroup(instance: unknown): boolean;
    supports(r: unknown): boolean;
    toObject(group: ReflectionGroup, obj: JSONReflection): JSONReflectionGroup;
}
